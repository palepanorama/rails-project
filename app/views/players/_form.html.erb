<%= form_for @player do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <br>
    <%= f.label :position %>
    <%= f.collection_select :position_id, Position.order(:name),:id,:name, include_blank: true %>
    <br>
    <%= f.submit %>
<% end %>